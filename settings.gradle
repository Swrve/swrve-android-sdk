pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

include ':SwrveSDKCommon', ':SwrveSDK', ':SwrveSDKTest'



// No samples are included here. Every sample under samples/ is a standalone project with its own
// settings.gradle and Gradle wrapper, consuming the published Swrve SDK artifact — open each one
// as its own project in Android Studio. To build a sample against local SDK source instead, see
// the commented includeBuild block in that sample's own settings.gradle.
