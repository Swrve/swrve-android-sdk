[versions]
agp = "8.13.1"
kotlin = "2.0.20"
coreKtx = "1.17.0"
activityCompose = "1.9.3"
compose-bom = "2025.11.01"
coil = "2.7.0"
swrveSdkVersion = "12.3.0"

[libraries]
androidx-core-ktx = { group = "androidx.core", name = "core-ktx", version.ref = "coreKtx" }
androidx-activity-compose = { group = "androidx.activity", name = "activity-compose", version.ref = "activityCompose" }
compose-bom = { group = "androidx.compose", name = "compose-bom", version.ref = "compose-bom" }
compose-ui = { group = "androidx.compose.ui", name = "ui" }
compose-material3 = { group = "androidx.compose.material3", name = "material3" }
compose-ui-tooling-preview = { group = "androidx.compose.ui", name = "ui-tooling-preview" }
compose-ui-tooling = { group = "androidx.compose.ui", name = "ui-tooling" }
# Just the two bottom-navigation icons. Version comes from the Compose BOM.
compose-material-icons-core = { group = "androidx.compose.material", name = "material-icons-core" }
# Loads the message thumbnails, which arrive as URLs. Any image loader would do; this is not a Swrve dependency.
coil-compose = { group = "io.coil-kt", name = "coil-compose", version.ref = "coil" }
# The inbox needs no push, so this sample uses the core artifact rather than swrve-firebase.
swrve-core = { group = "com.swrve.sdk.android", name = "swrve", version.ref = "swrveSdkVersion" }

[plugins]
android-application = { id = "com.android.application", version.ref = "agp" }
kotlin-android = { id = "org.jetbrains.kotlin.android", version.ref = "kotlin" }
kotlin-compose = { id = "org.jetbrains.kotlin.plugin.compose", version.ref = "kotlin" }
